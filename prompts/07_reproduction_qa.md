Act as the release and reproduction engineer. Build a clean one-command workflow, benchmark runtime
and memory, run the supplied validator, add Docker or a small-sample hosted demo, complete metadata,
and test from a clean checkout with network disabled during ranking. Reject release if any submission
rule or acceptance gate in AGENT_EXECUTION_PLAN.md fails.
